defaults:
  - experiment
  - serializer: ???
  - model: ???
  - override hydra/job_logging: colorlog
  - override hydra/hydra_logging: colorlog

checkpoint: ???
job_type: correct

hydra:
  searchpath:
    - file:///etc/mm
    - file://${oc.env:HOME}/.config/mm